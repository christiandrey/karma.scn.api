export namespace Methods {

    // -------------------------------------------------------------------------------------------------
    /** Fades an hex color */
    export function fade(hex: string, opacity: number = 1): string {
        return ""
    }
}