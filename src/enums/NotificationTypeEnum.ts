export enum NotificationTypeEnum {
    Default = 0,
    ConnectionRequest = 10,
    ConnectionResult = 20,
    WebinarAnnouncement = 30,
    WebinarStart = 40,
    VerificationSuccess = 50,
    Announcement = 60,
}